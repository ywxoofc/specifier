warn("Hat Geladen") local Players = game:GetService("Players") local ReplicatedStorage = game:GetService("ReplicatedStorage") local player = Players.LocalPlayer local remote = ReplicatedStorage.Shared.Teleport.StartRaid local health = workspace.Characters.o_Oywxo.HealthProperties.Health local rewards = player:WaitForChild("PlayerGui"):WaitForChild("MissionRewards"):WaitForChild("MissionRewards") local ids = {1005, 1006, 1007} local wasDead = false local wasVisible = false while true do if health.Value <= 0 and not wasDead then wasDead = true remote:FireServer(ids[math.random(#ids)], 5) elseif health.Value > 0 then wasDead = false end if rewards.Visible and not wasVisible then wasVisible = true remote:FireServer(ids[math.random(#ids)], 5) elseif not rewards.Visible then wasVisible = false end task.wait(0.1) end
